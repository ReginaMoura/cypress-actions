name: Cypress Cloud

on:
  workflow_dispatch: 
    inputs:
      spec:
        description: Run by cypress command line
        default: cypress/e2e/*
        
jobs:
  cypress-e2e-electron:
   runs-on: ubuntu-latest
   container: 
     image: cypress/browsers:node-22.19.0-chrome-139.0.7258.154-1-ff-142.0.1-edge-139.0.3405.125-1
     options: --user 1001
   steps:
     - name: Get code
       uses: actions/checkout@v6.0.2

     - name: Cypress run
       uses: cypress-io/github-action@v6.5.0
       with:
         install-command: yarn install
         browser: electron
         record: true
         group: UI Electron
         spec: ${{ github.event.inputs.spec }}
       env:
         CYPRESS_RECORD_KEY: fe93b225-68d7-48bd-ac0b-93b0174527dc
         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN}}
  cypress-e2e-chrome:
   needs: cypress-e2e-electron
   runs-on: ubuntu-latest
   container:
     image: cypress/browsers:node-22.19.0-chrome-139.0.7258.154-1-ff-142.0.1-edge-139.0.3405.125-1
     options: --user 1001
   steps:
     - name: Get code
       uses: actions/checkout@v6.0.2
       
     - name: Cypress run
       uses: cypress-io/github-action@v6.5.0
       with:
         install-command: yarn install
         browser: chrome
         record: true
         group: UI Chrome
         spec: ${{ github.event.inputs.spec }}
       env:
         CYPRESS_RECORD_KEY: fe93b225-68d7-48bd-ac0b-93b0174527dc
         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN}}
  cypress-e2e-firefox:
    needs: cypress-e2e-electron
    runs-on: ubuntu-latest
    container:
     image: cypress/browsers:node-22.19.0-chrome-139.0.7258.154-1-ff-142.0.1-edge-139.0.3405.125-1
     options: --user 1001
    steps:
     - name: Get code
       uses: actions/checkout@v6.0.2

     - name: Cypress run
       uses: cypress-io/github-action@v6.5.0
       with:
         install-command: yarn install
         browser: firefox
         record: true
         group: UI Firefox
         spec: ${{ github.event.inputs.spec }}
       env:
         CYPRESS_RECORD_KEY: fe93b225-68d7-48bd-ac0b-93b0174527dc
         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN}}
        
